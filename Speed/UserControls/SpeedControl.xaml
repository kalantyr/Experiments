<UserControl x:Class="Speed.UserControls.SpeedControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Speed" Style="{StaticResource BlockHeader}"/>
        <TextBlock x:Name="_tbSpeedMetersPerSecond" Grid.Row="1" Style="{StaticResource MainText}"/>
        <TextBlock x:Name="_tbSpeedKilometersPerHour" Grid.Row="2" Style="{StaticResource AdvancedInfo}"/>
    </Grid>
</UserControl>
